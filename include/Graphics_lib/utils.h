#ifndef PROJECT_QUIZ_FIGURE_UTILS_H
#define PROJECT_QUIZ_FIGURE_UTILS_H

const unsigned int SCR_WIDTH = 1280;
const unsigned int SCR_HEIGHT = 720; // или 1280? upd. 720 vse-taki (!) не трогайте udp2 grustno (

enum {
    NOTHING = 0, // NOTHING должен быть равен 0, не меняйте (!)
    START_GAME,
    REDACTOR,
    EXIT,
    MENU,
};

#endif //PROJECT_QUIZ_FIGURE_UTILS_H
